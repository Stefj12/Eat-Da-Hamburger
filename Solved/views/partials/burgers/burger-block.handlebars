<li>
	{{name}}

	<button class="change-hungry" data-id="{{id}}" data-newhungry="{{hungry}}">
		{{#if hungry}}HUNGRY TIME!{{else}}FULL!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
