<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#unless devoured}}DEVOUR TIME!{{/unless}}
	</button>

	<button class="delete-burger bt-color" data-id="{{this.id}}">DELETE!</button>
</li>
